# Go Task Management API

Данный репозиторий создан в рамках выполнения учебного задания по курсу Go. 
Цель проекта — разработка минималистичного backend-сервиса для управления задачами (Issues / Todos).

## 🚀 Реализованный функционал

### Основные требования (DoD):
- **HTTP API**: Написано с использованием только стандартной библиотеки `net/http`.
- **JSON**: Все запросы и ответы обрабатываются в формате JSON.
- **In-memory Storage**: Данные хранятся в памяти (map) с обеспечением потокобезопасности.
- **Middleware**:
    - **Logging**: Логирование каждого запроса (Метод, Путь, Время).
    - **Auth**: Защита эндпоинтов с помощью API Key (`X-API-KEY: secret12345`).

### Эндпоинты:
- `GET /tasks` — получить список всех задач.
- `GET /tasks?id=1` — получить конкретную задачу по ID.
- `POST /tasks` — создать новую задачу (Title — обязательное поле).
- `PATCH /tasks?id=1` — обновить статус задачи (`done: true/false`).
- `DELETE /tasks?id=1` — удалить задачу.

### Дополнительные фичи:
- 🟢 **Easy**: Фильтрация задач по статусу: `GET /tasks?done=true`.
- 🟡 **Medium**: Реализован метод `DELETE` и правильная структура проекта (handlers, middleware, models).
- 🔵 **Advanced**: 
    - Потокобезопасность через `sync.RWMutex`.
    - Плавная остановка сервера (**Graceful Shutdown**).
    - Кастомный роутинг без сторонних фреймворков.

## 🛠 Технологический стек
- **Язык**: Go 1.21+
- **Frameworks**: Нет (только Standard Library)
- **Порт**: 8080

## 📦 Структура проекта
```text
todo/
├── cmd/
│   └── api/
│       └── main.go          # Точка входа и Graceful Shutdown
├── internal/
│   ├── handlers/            # Логика обработки HTTP-запросов
│   ├── middleware/          # Auth и Logging
│   ├── models/              # Структуры данных
│   └── repository/          # Потокобезопасное хранилище (In-memory)
└── go.mod
🏃 Как запустить

    Клонируйте репозиторий:
    code Bash

    git clone https://github.com/callmetsoy/Golang.git

    Перейдите в папку проекта:
    code Bash

    cd todo

    Запустите сервер:
    code Bash

    go run ./cmd/api
